void main(List<String> args) {
  /// Specifying Data Types in DArt
  /// We can add new rule in our file analysis_options.yaml under the linter->rules such as always_specify_types, so that we always have to specifying the data type for all our variable im code,
  /// otherwise we'll get errors.
}
