void main(List<String> args) {
  /// Unary prefix Only on one value before one value
  /// Unary postfix (after one value)
  /// Binary infix (operate on two values )
  /// compound assignment
  ///
  int age3 = 30;
  print(--age3);
  print(++age3);
}
